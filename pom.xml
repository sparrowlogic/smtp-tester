<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.sparrowlogic</groupId>
    <artifactId>smtp-tester</artifactId>
    <version>0.1.0</version>
    <name>smtp-tester</name>
    <description>SMTP testing server with a web inbox, RFC compliance validation and an MCP server</description>
    <url>https://github.com/sparrowlogic/smtp-tester</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <organization>
        <name>Sparrow Logic, Inc.</name>
        <url>https://github.com/sparrowlogic</url>
    </organization>

    <developers>
        <developer>
            <id>jackdpeterson</id>
            <name>Jack Peterson</name>
            <url>https://github.com/jackdpeterson</url>
            <organization>Sparrow Logic, Inc.</organization>
            <organizationUrl>https://github.com/sparrowlogic</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/sparrowlogic/smtp-tester.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/sparrowlogic/smtp-tester.git</developerConnection>
        <tag>HEAD</tag>
        <url>https://github.com/sparrowlogic/smtp-tester</url>
    </scm>

    <properties>
        <java.version>26</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring-ai.version>2.0.1</spring-ai.version>
        <jte.version>3.2.4</jte.version>
        <jspecify.version>1.0.1</jspecify.version>
        <subethasmtp.version>7.2.2</subethasmtp.version>
        <bouncycastle.version>1.86</bouncycastle.version>

        <errorprone.version>2.50.0</errorprone.version>
        <nullaway.version>0.14.1</nullaway.version>
        <checkstyle.version>14.1.0</checkstyle.version>
        <spotless.version>3.10.2</spotless.version>
        <maven-reporting.version>3.6.0</maven-reporting.version>
        <!-- JaCoCo is on by default; opt out via `-Djacoco.skip=true` for fast loops. -->
        <jacoco.skip>false</jacoco.skip>
        <jacoco.version>0.8.15</jacoco.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Brings spring-boot-starter-web plus the streamable-HTTP MCP endpoint (/mcp).
             The web server and the MCP server deliberately share one port (8025) so a
             single `docker run -p 1025:1025 -p 8025:8025` exposes inbox + MCP + REST. -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>

        <!-- JTE runtime only, deliberately NOT jte-spring-boot-starter-3. The starter throws
             JteConfigurationException unless gg.jte.development-mode or use-precompiled-templates
             is set, and it registers TemplateEngine / ViewResolver beans we would then have to work
             around. Rendering through TemplateEngine.createPrecompiled() in our own controller keeps
             the bean graph to exactly what SmtpTesterConfiguration declares. -->
        <dependency>
            <groupId>gg.jte</groupId>
            <artifactId>jte-runtime</artifactId>
            <version>${jte.version}</version>
        </dependency>

        <!-- JSpecify supplies the @NullMarked / @Nullable vocabulary NullAway enforces. -->
        <dependency>
            <groupId>org.jspecify</groupId>
            <artifactId>jspecify</artifactId>
            <version>${jspecify.version}</version>
        </dependency>

        <!-- Maintained fork of SubEthaSMTP (jakarta.mail based). Supplies the RFC 5321
             conversation; everything above DATA is ours. -->
        <dependency>
            <groupId>com.github.davidmoten</groupId>
            <artifactId>subethasmtp</artifactId>
            <version>${subethasmtp.version}</version>
        </dependency>

        <!-- subethasmtp declares jakarta.mail-api as `provided`, so we must declare it
             ourselves. angus-mail is the implementation that backs MimeMessage parsing. -->
        <dependency>
            <groupId>jakarta.mail</groupId>
            <artifactId>jakarta.mail-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-mail</artifactId>
        </dependency>

        <!-- Only used to mint the self-signed STARTTLS certificate at startup. The JDK can read
             and use an X.509 certificate but cannot create one without reaching into sun.security
             internals, and a test tool must not ship a committed private key. Optional at runtime:
             with TLS off, or with a keystore supplied, nothing here is loaded. -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- The plain `smtp-tester-<version>.jar` stays a normal library artifact so it
                 can be added as a <dependency> (auto-configuration starts the SMTP server
                 inside a host app's tests). The executable fat jar is published alongside it
                 under the `exec` classifier and is what the Docker image runs. -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <classifier>exec</classifier>
                    <mainClass>com.sparrowlogic.smtptester.SmtpTesterApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Compile-time null-safety: ErrorProne + NullAway in JSpecify mode.
                 OnlyNullMarked=true means only @NullMarked packages are checked; every
                 package in this project is @NullMarked via package-info.java, so this is
                 effectively whole-project enforcement. The add-exports / add-opens flags +
                 fork=true are required by ErrorProne to reach javac internals (JEP 396). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.google.errorprone</groupId>
                            <artifactId>error_prone_core</artifactId>
                            <version>${errorprone.version}</version>
                        </path>
                        <path>
                            <groupId>com.uber.nullaway</groupId>
                            <artifactId>nullaway</artifactId>
                            <version>${nullaway.version}</version>
                        </path>
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                            <version>${project.parent.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <fork>true</fork>
                    <compilerArgs>
                        <arg>-Xlint:unchecked</arg>
                        <arg>-XDcompilePolicy=simple</arg>
                        <arg>--should-stop=ifError=FLOW</arg>
                        <arg>-Xplugin:ErrorProne -XepDisableAllChecks -XepOpt:NullAway:OnlyNullMarked=true -XepOpt:NullAway:JSpecifyMode=true -XepOpt:NullAway:CustomContractAnnotations=org.springframework.lang.Contract -Xep:NullAway:ERROR -XepExcludedPaths:(.*/test/java/.*|.*/target/generated-sources/.*|.*/target/jte-classes/.*)</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                        <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                        <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- JTE precompilation. Without this the view resolver would need src/main/jte
                 on disk at runtime, which does not exist inside the container. -->
            <plugin>
                <groupId>gg.jte</groupId>
                <artifactId>jte-maven-plugin</artifactId>
                <version>${jte.version}</version>
                <configuration>
                    <sourceDirectory>${project.basedir}/src/main/jte</sourceDirectory>
                    <contentType>Html</contentType>
                </configuration>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <removeUnusedImports/>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                        <indent>
                            <spaces>true</spaces>
                            <spacesPerTab>4</spacesPerTab>
                        </indent>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <skip>${jacoco.skip}</skip>
                    <!-- The application class is a main method and a properties map; the JTE
                         classes are generated. Neither is code a test could meaningfully assert on,
                         and counting them would only let real gaps hide behind a passing ratio. -->
                    <excludes>
                        <exclude>**/SmtpTesterApplication.class</exclude>
                        <exclude>gg/jte/generated/**</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <haltOnFailure>true</haltOnFailure>
                            <rules>
                                <!-- 85% line coverage is the project's stated floor, enforced here
                                     rather than only written down, so `mvn verify` is the whole
                                     answer to "is this tested enough". Both numbers sit below what
                                     the suite currently achieves: a gate set at the current figure
                                     fails on unrelated refactors and gets lowered, which is how a
                                     coverage gate stops meaning anything. -->
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.85</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.75</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-reporting.version}</version>
                <configuration>
                    <configLocation>checkstyle.xml</configLocation>
                    <suppressionsLocation>checkstyle-suppressions.xml</suppressionsLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <violationSeverity>warning</violationSeverity>
                    <excludes>**/jte-classes/**,**/Jte*Generated.java</excludes>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>checkstyle</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
